// 載入所需 model

// 設計 todoServices
const todoServices = {
  getTodos: (req, cb) => cb(null, { messages: '功能開發中' }),
  postTodo: (req, cb) => cb(null, { messages: '功能開發中' }),
  patchTodo: (req, cb) => cb(null, { messages: '功能開發中' }),
  deleteTodo: (req, cb) => cb(null, { messages: '功能開發中' })
}

module.exports = todoServices
